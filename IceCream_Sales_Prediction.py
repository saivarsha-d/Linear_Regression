# -*- coding: utf-8 -*-
"""IceCreamSalesPredict.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1xhabfTqBDkiLNQcZXzxU9kuzxK3lvn7r
"""

import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
from sklearn.metrics import r2_score

df = pd.read_csv("IceCream.csv")
df

x = df.iloc[:, 0:1].values
y = df.iloc[:, 1].values

x.shape

y.shape

from sklearn.model_selection import train_test_split
x_train, x_test, y_train, y_test = train_test_split(x, y, test_size=0.25, random_state=2)

x_train.shape

x_test.shape

from sklearn.linear_model import LinearRegression

model = LinearRegression()
model.fit(x_train, y_train)

model.intercept_

model.coef_

model.score(x_train, y_train)

model.score(x_test, y_test)

y_pred = model.predict(x_test)
score = r2_score(y_test, y_pred)
print(score)

plt.scatter(x_train,y_train,color='red')
plt.plot(x_train,model.predict(x_train),color='blue')
plt.title("Training Data")
plt.xlabel("Temperature")
plt.ylabel("Revenue")
plt.show()

plt.scatter(x_test,y_test,color='red')
plt.plot(x_test,model.predict(x_test),color='blue')
plt.title("Testing Data")
plt.xlabel("Temperature")
plt.ylabel("Revenue")
plt.show()



new_temp = 28.0

predicted_rev = model.predict(np.array(new_temp).reshape(-1, 1))

print(f"For a temperature of {new_temp}°C, the predicted revenue is ${predicted_rev[0]:.2f}")

